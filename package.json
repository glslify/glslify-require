{
  "name": "glslify-require",
  "version": "1.0.2",
  "description": "Browserify plugin to require() OpenGL Shading Language (GLSL) files",
  "keywords": [
    "browserify",
    "browserify-plugin",
    "glsl"
  ],
  "main": "src/index.js",
  "scripts": {
    "pretest": "npm prune && npm update",
    "test": "tap test/*.js --cov"
  },
  "author": "Eric MORAND",
  "repository": "ericmorand/glslify-require",
  "license": "Apache-2.0",
  "dependencies": {
    "event-emitter": "^0.3.4",
    "glslify": "^6.0.1",
    "glslify-deps": "^1.3.0",
    "through2": "^2.0.3"
  },
  "devDependencies": {
    "browserify": "^14.1.0",
    "tap": "^10.3.0"
  }
}
